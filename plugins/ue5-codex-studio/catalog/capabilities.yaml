schema_version: 2
statuses: [DIRECT, SCRIPT_FALLBACK, THIN_MCP_EXTENSION, MANUAL_REQUIRED, UNSUPPORTED]
availability: [AVAILABLE, CANARY_ONLY, BLOCKED, MANUAL, UNSUPPORTED]
capabilities:
  - {id: narrative.trace_change, mutability: WRITE, status: DIRECT, availability: AVAILABLE, backend: studio, evidence: OBSERVED, scope: trace_records, executor: built_in, fallback: manual_yaml}
  - {id: ue.level.read, mutability: READ, status: THIN_MCP_EXTENSION, availability: CANARY_ONLY, backend: ue5ultimatemcp-controlled, evidence: OBSERVED, scope: canary_and_read_only, executor: controlled_adapter, fallback: editor_manual}
  - {id: ue.actor.read, mutability: READ, status: THIN_MCP_EXTENSION, availability: CANARY_ONLY, backend: ue5ultimatemcp-controlled, evidence: OBSERVED, scope: canary_and_read_only, executor: controlled_adapter, fallback: editor_manual}
  - {id: ue.viewport.capture, mutability: READ, status: THIN_MCP_EXTENSION, availability: CANARY_ONLY, backend: ue5ultimatemcp-controlled, evidence: OBSERVED, scope: canary_and_read_only, executor: controlled_adapter, fallback: editor_manual}
  - {id: ue.canary.actor.create, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: CANARY_ONLY, backend: ue5ultimatemcp-controlled, evidence: PERSISTED, scope: dedicated_canary_map_only, executor: controlled_adapter, fallback: editor_manual}
  - {id: ue.blueprint.edit, mutability: WRITE, status: UNSUPPORTED, availability: UNSUPPORTED, backend: none, evidence: PERSISTED, scope: none, executor: null, fallback: editor_manual}
  - {id: ue.blueprint.create_from_template, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: ue5ultimatemcp-controlled, evidence: PERSISTED, scope: approved_templates_only, executor: server_allowlist_pending_canary, fallback: editor_manual}
  - {id: ue.blueprint.add_allowlisted_component, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: ue5ultimatemcp-controlled, evidence: PERSISTED, scope: approved_component_classes_only, executor: server_allowlist_pending_canary, fallback: editor_manual}
  - {id: ue.blueprint.set_allowlisted_variable, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: ue5ultimatemcp-controlled, evidence: PERSISTED, scope: approved_variable_types_only, executor: server_allowlist_pending_canary, fallback: editor_manual}
  - {id: ue.blueprint.apply_allowlisted_graph_patch, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: ue5ultimatemcp-controlled, evidence: PERSISTED, scope: approved_graph_patches_with_approval, executor: server_allowlist_pending_canary, fallback: editor_manual}
  - {id: ue.blueprint.compile, mutability: EXECUTE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: ue5ultimatemcp-controlled, evidence: PERSISTED, scope: selected_blueprint_compile_only, executor: server_allowlist_pending_canary, fallback: editor_manual}
  - {id: ue.blueprint.save_reload_readback, mutability: EXECUTE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: ue5ultimatemcp-controlled, evidence: PERSISTED, scope: selected_blueprint_readback_only, executor: server_allowlist_pending_canary, fallback: editor_manual}
  - {id: ue.automation.run, mutability: EXECUTE, status: SCRIPT_FALLBACK, availability: BLOCKED, backend: fixed_ue_cli, evidence: RUNTIME, scope: allowlisted_project_smoke_and_automation_filters, executor: run_ue_automation_py, fallback: editor_manual}
  - {id: ue.package.build, mutability: EXECUTE, status: SCRIPT_FALLBACK, availability: BLOCKED, backend: fixed_uat_cli, evidence: RUNTIME, scope: win64_shipping_only, executor: build_ue_package_py, fallback: manual_uat}
  - {id: ue.paper2d.author, mutability: WRITE, status: UNSUPPORTED, availability: UNSUPPORTED, backend: none, evidence: PERSISTED, scope: none, executor: null, fallback: editor_manual}
  - {id: ue.mass.spawn, mutability: WRITE, status: UNSUPPORTED, availability: UNSUPPORTED, backend: none, evidence: RUNTIME, scope: none, executor: null, fallback: editor_manual}
  - {id: blender.discovery, mutability: READ, status: DIRECT, availability: BLOCKED, backend: blender-mcp, evidence: OBSERVED, scope: discovery, executor: gateway_pending_canary, fallback: blender_manual}
  - {id: blender.asset.author, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: blender-mcp, evidence: PERSISTED, scope: new_assets_only, executor: gateway_pending_canary, fallback: blender_manual}
  - {id: dcc.model.create_mesh, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: blender-mcp, evidence: PERSISTED, scope: project_dcc_workspace_new_meshes, executor: gateway_pending_canary, fallback: blender_manual}
  - {id: dcc.model.create_sculpt, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: blender-mcp, evidence: PERSISTED, scope: project_dcc_workspace_new_sculpts, executor: gateway_pending_canary, fallback: blender_manual}
  - {id: dcc.asset.import_new, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: blender-mcp, evidence: PERSISTED, scope: project_dcc_workspace_new_imports, executor: gateway_pending_canary, fallback: blender_manual}
  - {id: dcc.model.modify_existing, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: blender-mcp, evidence: PERSISTED, scope: approved_project_dcc_existing_meshes, executor: gateway_pending_canary, fallback: blender_manual}
  - {id: dcc.asset.delete, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: blender-mcp, evidence: PERSISTED, scope: approved_project_dcc_deletions, executor: gateway_pending_canary, fallback: blender_manual}
  - {id: cloud.asset.concept.create, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: atlascloud-controlled, evidence: OBSERVED, scope: new_candidates, executor: approval_gated_adapter, fallback: manual_provider}
  - {id: cloud.asset.concept.modify, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: atlascloud-controlled, evidence: OBSERVED, scope: registered_assets_with_approval, executor: approval_gated_adapter, fallback: manual_provider}
  - {id: cloud.asset.mesh.create, mutability: WRITE, status: THIN_MCP_EXTENSION, availability: BLOCKED, backend: atlascloud-controlled, evidence: OBSERVED, scope: new_candidates, executor: approval_gated_adapter, fallback: manual_provider}
  - {id: audio.music_production, mutability: WRITE, status: MANUAL_REQUIRED, availability: MANUAL, backend: provider-neutral, evidence: RUNTIME, scope: music, executor: null, fallback: manual_provider}
  - {id: audio.voice_production, mutability: WRITE, status: MANUAL_REQUIRED, availability: MANUAL, backend: provider-neutral, evidence: RUNTIME, scope: voice, executor: null, fallback: manual_provider}
